# Claude Guidelines

## Purpose
This repository is used for tracing and studying Trino's source code. All work here is read-only research and note-taking.

## Rules

1. **Read-only submodule**: NEVER commit any changes to the `trino/` submodule. Do not stage, commit, or push anything inside the `trino/` directory. It exists solely for reading and reference.
